{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Utilities",
            "type": "debugpy",
            "request": "launch",
            "module": "Utilities.run",
            "cwd": "${workspaceFolder}"
        }
    ]
}